# System Design

The platform separates transaction scoring, explanation generation, audit persistence, and the React review interface. The initial API uses a deterministic demo scorer until trained artifacts are supplied.
